const stabilaWebBuilder = require('./stabilaWebBuilder');
const stabilaWeb = stabilaWebBuilder.createInstance();

const amount = process.argv[2] || 10;

// (async function () {
//     await stabilaWebBuilder.newTestAccounts(amount)
// })()

